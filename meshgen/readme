// ===================================
//  MESH GENERATION PROGRAM WITH SIMMETRIX
// ===================================

// install PUMI with Simmetrix  on SCOREC linux
   - see config file in config-files/hostname-pumi-sim-config.sh

  $cd build
  $../config.sh
  $make install


// install m3dc1_meshgen

  - see config file in config-files/hostname-sim-config.sh

  $cd build
  $../config.sh
  $make install

  ==> executable file "m3dc1_meshgen" is installed in the current "build" folder

// run mesh generation program on SCOREC
  module load simmetrix/simModSuite/11.0-170826
  /usr/local/openmpi/latest/bin/mpirun -np 1 ./m3dc1_meshgen input-file


