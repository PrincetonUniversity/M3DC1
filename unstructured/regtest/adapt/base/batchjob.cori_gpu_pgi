#!/bin/bash
#SBATCH --account=m1759
#SBATCH -J m3dc1_regtest_adapt
#SBATCH --nodes=1
#SBATCH --tasks-per-node=16
#SBATCH -C gpu
#SBATCH -G 8
#SBATCH -t 0:29:59
#SBATCH -o C1stdout

touch started

# partition mesh
#part_mesh.sh circle-0.02-0.0-0.0-2K.smb 16
#echo $? > mesh_partitioned

# Run M3D-C1
$M3DC1_MPIRUN -n 16 m3dc1_2d
$M3DC1_MPIRUN -n 16 collapse circle-0.02-0.0-0.0.dmg ts0-adapted.smb mesh.smb 16

if ! [ -f mesh0.smb ]; then
    echo 0 > C1ke
fi

touch finished
